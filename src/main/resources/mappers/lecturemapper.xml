<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.mapper.lectureMapper">
    <select id="readBasicData" resultType="LectureDto">
        select * from lecture
    </select>
    <insert id="insertLecture">
        insert into lecture(lecCategory, lecName, lecPrice, lecRegDate)
        VALUES(#{category}, #{name}, #{price}, #{regDate})
    </insert>
</mapper>
